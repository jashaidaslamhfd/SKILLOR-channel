# Core AI/LLM APIs
groq>=0.9.0
google-genai>=0.5.0

# Text-to-Speech
# Chatterbox = primary (MIT license, emotion-exaggeration control - matches
# the dark/mystery tone). Kokoro = automatic fallback if Chatterbox fails
# to load or generate. See src/voice_generator.py for the fallback logic.
chatterbox-tts>=0.1.7
# Chatterbox needs this for its internal audio watermarker
# (self.watermarker = perth.PerthImplicitWatermarker()). If this package is
# missing or a name-clashing "perth" package gets installed instead,
# ChatterboxTTS.from_pretrained() fails with:
#   TypeError: 'NoneType' object is not callable
# Pinning it explicitly here guarantees the real package is always present.
resemble-perth>=1.0.1
kokoro==0.9.4
# Silences a FutureWarning from diffusers (used internally by Chatterbox):
# "LoRACompatibleLinear is deprecated... switch to PEFT backend". Cosmetic
# only (doesn't affect output), but this is the documented fix.
peft>=0.10.0

# Audio Processing
soundfile>=0.12.1

# ML/Deep Learning
torch>=2.1.2
torchaudio>=2.1.0
transformers>=4.40.0

# Video Processing
# MoviePy 2.x breaks `from moviepy.editor import ...` (the .editor
# subpackage was removed) and changes fx import paths. Pinning to 1.x
# until the codebase migrates to MoviePy 2.x API. See:
#   https://zulko.github.io/moviepy/install.html#migration-from-1-x-to-2-x
moviepy>=1.0.3,<2.0.0
imageio-ffmpeg>=0.4.8

# Image Processing
# Pillow 10+ removed Image.ANTIALIAS; this codebase already uses
# Image.LANCZOS everywhere, so 10.x is fully compatible.
Pillow>=10.0.0
numpy>=1.24.0

# HTTP/API Requests
requests>=2.31.0

# Browser automation (screenshot fallback layer for scene visuals)
playwright>=1.44.0

# Google APIs
google-api-python-client>=2.100.0
google-auth>=2.25.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# Timezone handling
pytz>=2024.1

# Utilities
python-dotenv>=1.0.0
